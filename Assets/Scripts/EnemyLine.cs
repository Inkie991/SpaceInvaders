using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyLine : MonoBehaviour
{
    /*[SerializeField] private List<GameObject> _enemiesInLine;

    public bool isEmpty
    {
        get
        {
            if (_enemiesInLine.Count == 0)
            {
                return true;
            }
            else
            {
                return false;
            }
        }
    }

    void Update()
    {
        foreach (var enemy in _enemiesInLine)
        {
            if (enemy == null)
            {
                _enemiesInLine.Remove(enemy);
            }
        }
    } */
}
